You are implementing a minimal, production-quality Python library.

Constraints:
- Write the smallest amount of code that satisfies the requirements.
- No unnecessary abstractions.
- No speculative features.
- Use standard library unless explicitly needed.
- Include tests (pytest).
- Keep functions small and pure where possible.
- Avoid duplication.
- Prefer explicit over implicit behavior.
- Output only code (no explanations).